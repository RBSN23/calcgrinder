import { ResetPasswordForm } from './reset-password-form';

export const metadata = {
  title: 'Set a new password · Calcgrinder',
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
